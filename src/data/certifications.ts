export type Certification = { id: string; title: string; issuer: string; date?: string; credentialId?: string; credentialUrl?: string; image?: string };
export const certifications: Certification[] = [];
